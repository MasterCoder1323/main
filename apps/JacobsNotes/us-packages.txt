#include <iostream>
int main() {
	using namespace std;
	cout << "These are useful Packages to install and what they do.\n";
	cout << "$ sudo apt-get install build-essential   | this package is used for makeing debian packages. to see how: use the command\n $ ht-mk-deb\n";
	cout << "$ sudo apt install nano                  | this is a better in-terminal editor than vim. to acsess use\n $ nano (filename)\n";
	cout << "$ sudo apt install gnome-software        | this is a good linux app store.\n";
}
